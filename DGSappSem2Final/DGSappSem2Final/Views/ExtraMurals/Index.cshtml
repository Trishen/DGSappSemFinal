@model IEnumerable<DGSappSem2Final.Models.Murals.ExtraMural>

@{
    ViewBag.Title = "Index";
}

<h2>Extra Murals</h2>

<p>
    @Html.ActionLink("Add a New Extra Murals", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.MuralName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.MuralName)
        </td>
        <td>
            @*@Html.ActionLink("Edit", "Edit", new { id=item.MuralId }) |*@
            @Html.ActionLink("Details", "Details", new { id=item.MuralId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MuralId })
        </td>
    </tr>
}

</table>
